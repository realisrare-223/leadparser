# ============================================================
# LeadParser — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit your .env file to version control.
#
# Most settings live in config.yaml.
# This file is for secrets / machine-specific overrides only.
# ============================================================

# ── Google Sheets credentials ────────────────────────────────
# Path to your service account JSON file (downloaded from Google Cloud Console)
# Default is credentials.json in the project root.
# Only override this if your file is stored elsewhere.
# GOOGLE_CREDENTIALS_PATH=credentials.json

# ── Optional overrides ────────────────────────────────────────
# Override config.yaml settings at runtime without editing the file.
# Uncomment and set as needed.

# LEADPARSER_CITY=Austin
# LEADPARSER_STATE=TX
# LEADPARSER_HEADLESS=true
# LEADPARSER_LOG_LEVEL=DEBUG
